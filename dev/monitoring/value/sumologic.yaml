sumologic:
  accessId: "suaRNxrpP04FUL"
  accessKey: "Gn7Q6LP3DG06URgUZcUsW1OfegH44OBUnezIdps5LKKSzHFQzVNAo75E1aKMYxZv"
  clusterName: "dev-monitoring"
  logs:
    container:
      sourceCategory: "%{cluster}/%{namespace}/%{pod_name}"
      sourceName: "%{namespace}.%{pod}.%{container}"
    systemd:
      sourceCategory: "%{cluster}/systemd"
      sourceName: "k8s_systemd"
    kubelet:
      sourceCategory: "%{cluster}/kubelet"
      sourceName: "k8s_kubelet"

otellogs:
  enabled: true

otelmetrics:
  enabled: false

oteltraces:
  enabled: false

opentelemetry-operator:
  enabled: false

prometheus:
  enabled: false

fluent-bit:
  enabled: false

falco:
  enabled: false

kube-state-metrics:
  enabled: false

prometheus-node-exporter:
  enabled: false

telegraf-operator:
  enabled: false
